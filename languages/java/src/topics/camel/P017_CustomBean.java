package topics.camel;

import org.apache.camel.Exchange;

// from(direct:start).to(P017_CustomBean.class)
public class P017_CustomBean {
    public void doSomething(Exchange exchange) {
        // Custom code
    }
}
