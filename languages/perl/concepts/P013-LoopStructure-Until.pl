# Description: This program explains a looping structure - until loop

# until keeps iterating UNTIL SOMETHING BECOMES TRUE
$var = 7;
until ($var < 5) {
    print "Value of variable is $var\n";
    $var--;
}
