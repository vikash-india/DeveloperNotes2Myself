# Description: This program explains a looping structure - do-until loop

# do-until keeps iterating UNTIL SOMETHING BECOMES TRUE
$var = 4;
do {
    print "Value of variable is $var\n";
    $var1--;
} until ($var < 5);
