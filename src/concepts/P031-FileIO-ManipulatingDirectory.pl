# This program explains how to manipulate the directory and the files

# Creates a directory
mkdir("fred") || die "Could not delete the directory: $!" ;

# Removes the directory
rmdir("fred") || die "Could not delete the directory: $!" ;



